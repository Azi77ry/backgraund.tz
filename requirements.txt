Flask==2.3.2
rembg==2.0.50
Pillow==10.0.0
gunicorn==20.1.0  # Production server for Render
werkzeug==2.3.7   # WSGI utility (bundled with Flask)
python-dotenv==1.0.0  # Optional (for local .env files)